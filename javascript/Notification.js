class Notification {
    constructor(title, body, priority){
        this.title = title; this.body = body; this.priority = priority;
    }

    getHTML(){
        if (this.priority){
            var html = `
            
            `
        }
    }
}